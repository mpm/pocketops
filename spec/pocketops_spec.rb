require 'spec_helper'

describe Pocketops do
  it 'has a version number' do
    expect(Pocketops::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
